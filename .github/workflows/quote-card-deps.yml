name: Quote card dependencies

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/quote-card-deps.yml
      - app.config.js

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm install --package-lock-only --ignore-scripts expo-sharing@~57.0.20 react-native-view-shot@5.1.0
      - name: Commit dependency updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- package.json package-lock.json; then
            exit 0
          fi
          git add package.json package-lock.json
          git commit -m "Install quote card image export dependencies"
          git push
