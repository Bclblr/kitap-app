name: Apply private profile UI
on:
  push:
    paths:
      - 'scripts/apply-private-profile-ui.cjs'
      - '.github/workflows/apply-private-profile-ui.yml'
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Apply private profile UI patch
        run: node scripts/apply-private-profile-ui.cjs
      - name: Install dependencies
        run: npm ci
      - name: TypeScript
        run: npm run typecheck
      - name: Route audit
        run: npm run audit:routes
      - name: Reader security tests
        run: npm run test:security
      - name: Commit changes
        run: |
          if git diff --quiet -- src/app/profile.tsx; then
            echo 'No profile UI changes to commit.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/app/profile.tsx
          git commit -m 'Show private profile lock state'
          git push
